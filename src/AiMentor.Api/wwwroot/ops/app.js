"use strict";
const state={type:"",items:[],cursor:null,selected:null,loading:false};
const $=selector=>document.querySelector(selector);
const tbody=$("#task-list tbody"),status=$("#status"),more=$("#more");
const labels={approval:"审批",execution:"执行",compensation:"补偿",incident:"故障"};
function safeDate(value){return value?new Intl.DateTimeFormat("zh-CN",{dateStyle:"short",timeStyle:"short"}).format(new Date(value)):"未设置"}
function setConnection(ok,text){$("#signal").classList.toggle("ok",ok);$("#connection").textContent=text}
async function load(append=false){if(state.loading)return;state.loading=true;status.textContent="正在加载任务";try{const query=new URLSearchParams({limit:"50"});if(state.type)query.set("type",state.type);if(append&&state.cursor)query.set("cursor",state.cursor);const response=await fetch(`/api/v1/operations/tasks?${query}`,{headers:{Accept:"application/json"}});if(response.status===401)throw new Error("身份已过期，请重新登录。 [401]");if(response.status===403)throw new Error("当前身份无权访问运营任务。 [403]");if(!response.ok)throw new Error(`任务服务暂不可用。 [${response.status}]`);const page=await response.json();state.items=append?state.items.concat(page.items):page.items;state.cursor=page.nextCursor;render();setConnection(true,"连接正常")}catch(error){status.textContent=error.message;setConnection(false,"连接异常")}finally{state.loading=false}}
function render(){tbody.replaceChildren();for(const item of state.items){const tr=document.createElement("tr");if(item.overdue)tr.className="overdue";if(state.selected?.id===item.id)tr.classList.add("selected");const type=document.createElement("td");type.textContent=labels[item.type]||item.type;const stat=document.createElement("td");const badge=document.createElement("span");badge.className=`badge${item.overdue?" risk":""}`;badge.textContent=item.status;stat.append(badge);const due=document.createElement("td");due.textContent=safeDate(item.dueAt);const id=document.createElement("td");const button=document.createElement("button");button.className="task-button";button.textContent=item.id;button.setAttribute("aria-label",`查看任务 ${item.id}`);button.onclick=()=>select(item,tr);id.append(button);tr.append(type,stat,due,id);tbody.append(tr)}status.textContent=state.items.length?`已载入 ${state.items.length} 项任务`:"当前过滤条件下没有任务。";more.hidden=!state.cursor;for(const key of Object.keys(labels))$(`#count-${key}`).textContent=state.items.filter(x=>x.type===key).length;$("#count-all").textContent=state.items.length}
function select(item,row){state.selected=item;tbody.querySelectorAll("tr").forEach(x=>x.classList.remove("selected"));row.classList.add("selected");$("#empty-detail").hidden=true;const dl=$("#detail-data");dl.hidden=false;dl.replaceChildren();for(const [name,value] of [["任务标识",item.id],["类型",labels[item.type]||item.type],["状态",item.status],["创建时间",safeDate(item.createdAt)],["更新时间",safeDate(item.updatedAt)],["截止时间",safeDate(item.dueAt)],["并发标记",item.eTag]]){const dt=document.createElement("dt"),dd=document.createElement("dd");dt.textContent=name;dd.textContent=value;dl.append(dt,dd)}renderActions(item)}
function renderActions(item){const box=$("#actions"),row=box.querySelector(".action-row");row.replaceChildren();box.hidden=!item.allowedActions.length;for(const action of item.allowedActions){const button=document.createElement("button");button.textContent={approve:"批准",reject:"拒绝",probe:"目标探测",review:"人工复核",resume:"继续排查",cancel:"取消运行"}[action]||action;button.className=action==="approve"?"primary":action==="reject"||action==="cancel"?"danger":"";button.onclick=()=>act(item,action,button);row.append(button)}}
async function act(item,action,button){const message=$("#action-message");if(item.type!=="approval"||!(action==="approve"||action==="reject")){message.textContent="该动作需要目标参数或最新版本，请进入对应工作流详情完成。";return}const reason=$("#reason").value.trim();if(!reason){message.textContent="请填写裁决说明。";$("#reason").focus();return}button.disabled=true;try{const response=await fetch(`/api/v1/tool-approvals/${encodeURIComponent(item.id)}/decision`,{method:"POST",headers:{"Content-Type":"application/json","If-Match":item.eTag},body:JSON.stringify({approved:action==="approve",reason})});if(response.status===409)throw new Error("任务已被其他人员更新，请刷新后重试。 [409]");if(response.status===401||response.status===403)throw new Error(`当前会话不能执行此操作。 [${response.status}]`);if(!response.ok)throw new Error(`操作未完成。 [${response.status}]`);message.textContent="裁决已提交。";await load()}catch(error){message.textContent=error.message}finally{button.disabled=false}}
$("#filters").addEventListener("click",event=>{const button=event.target.closest("button[data-type]");if(!button)return;$("#filters").querySelectorAll("button").forEach(x=>x.classList.remove("active"));button.classList.add("active");state.type=button.dataset.type;state.cursor=null;state.selected=null;load()});$("#refresh").onclick=()=>load();more.onclick=()=>load(true);document.addEventListener("visibilitychange",()=>{if(!document.hidden)load()});let timer=setInterval(()=>{if(!document.hidden)load()},30000);window.addEventListener("beforeunload",()=>clearInterval(timer));load();
