# 工程协作规范

## 代码注释

- 所有代码注释必须使用中文；专有名词、协议名、类型名和稳定错误码可以保留英文。
- 当设计原因、不变量、失败模式或安全边界无法从代码本身直接看出时，必须添加简洁注释。
- 安全、授权、耐久状态转换、评测门禁和恢复逻辑必须说明为何要失败关闭，或为何某个值不能被当作成功。
- 对 `NotReady`、指标分母排除、关键用例阻断等容易被误解的评测语义，必须在对应代码附近说明原因。
- 注释应紧邻其保护的规则；行为变化时同步更新或删除过期注释。
- 禁止逐行翻译语法、赋值、方法调用或显而易见的控制流。能通过清晰命名表达时，优先改进命名。
- 测试名称应直接描述所保护的回归或不变量；仅当测试夹具表达了不直观的失败场景时才添加注释。

## GitHub 交付

- 每次任务完成并通过适用的测试、格式化与安全检查后，必须提交本次改动并推送到 GitHub 仓库。
- 提交前必须检查工作区差异，保留用户已有改动，不得为了形成提交而回退或覆盖不属于本次任务的内容。
- 提交信息应准确概括已验证的行为变化；测试失败、检查未完成或仍有已知阻断时，不得把半成品作为完成结果推送。
- 推送失败时必须明确报告本地提交状态和失败原因，不能把仅存在于本地的提交描述为已经交付到 GitHub。
